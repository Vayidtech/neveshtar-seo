# Note

Core multi-provider logic is in:
- `src/lib/rewrite-article.ts`
- `src/components/seo-studio.tsx` (full UI with settings, history, downloads)

Full project zip is also available from the conversation artifacts.

To get the complete source including all auth/app-data scaffolding, download the zip from the project and replace or merge.
